pipeline {
     agent any
     stages {
         stage('Lint HTML' 'Upload to AWS') {
             steps {
                 sh 'echo "Hello Wolrd"'
                 sh '''
                     echo "Multiline shell steps works too"
                     ls -lah
                 '''
             }
         }
     }
}
